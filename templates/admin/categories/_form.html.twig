{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success btn-sm mt-4 ml-2 mb-5">{{ button_label|default('Save') }}</button>
    <a class="btn btn-primary btn-sm mt-4 ml-2 mb-5" href="{{ path('admin_categories') }}">back to list</a>
{{ form_end(form) }}
