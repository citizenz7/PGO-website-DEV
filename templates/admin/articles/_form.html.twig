{{ form_start(form) }}
    {{ form_widget(form, {'attr': {'class': 'form-group'}}) }}
    <button class="btn btn-success mt-4 mb-5">{{ button_label|default('Save') }}</button> 
    <a class="btn btn-primary mt-4 ml-2 mb-5" href="{{ path('admin_articles') }}">back to list</a>
{{ form_end(form) }}
